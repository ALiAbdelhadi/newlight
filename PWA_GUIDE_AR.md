# دليل تحويل التطبيقات إلى PWA (Progressive Web Apps)

## ✅ ما تم إنجازه

تم تحويل كلا التطبيقين (`www` و `admin`) إلى Progressive Web Apps (PWA) بحيث يمكن للمستخدمين تثبيتها وتنزيلها على أجهزتهم.

### التطبيق الرئيسي (www)

✅ **تم إكمال الإعدادات التالية:**
- ملف `manifest.ts` محسّن مع جميع الأيقونات المطلوبة
- Service Worker مسجل ويعمل تلقائياً
- Metadata و Viewport محسّنة لـ PWA
- إعدادات `next.config.ts` محسّنة مع runtime caching
- الأيقونات موجودة بالفعل (192x192 و 512x512)

### تطبيق الإدارة (admin)

✅ **تم إكمال الإعدادات التالية:**
- ملف `manifest.ts` جديد مع إعدادات PWA
- Service Worker مسجل في layout
- Metadata و Viewport محسّنة
- إعدادات `next.config.ts` محسّنة مع runtime caching
- ملف `sw.js` أساسي للـ service worker

⚠️ **مطلوب:** إضافة الأيقونات (راجع `apps/admin/PWA_SETUP.md`)

## 📱 كيفية استخدام PWA

### للمستخدمين:

1. **على Chrome/Edge (Desktop):**
   - افتح الموقع
   - انقر على أيقونة "Install" في شريط العنوان
   - أو اذهب إلى القائمة (⋮) > "Install [اسم التطبيق]"

2. **على Chrome (Android):**
   - افتح الموقع
   - ستظهر رسالة "Add to Home screen"
   - أو اذهب إلى القائمة > "Add to Home screen"

3. **على Safari (iOS):**
   - افتح الموقع
   - انقر على زر المشاركة (Share)
   - اختر "Add to Home Screen"

### للمطورين:

#### بناء التطبيقات:

```bash
# بناء تطبيق www
cd apps/www
pnpm build
pnpm start

# بناء تطبيق admin
cd apps/admin
pnpm build
pnpm start
```

#### ملاحظات مهمة:

- ⚠️ **PWA يعمل فقط في وضع الإنتاج (Production)**
- في وضع التطوير (Development)، Service Worker معطل تلقائياً
- يجب بناء التطبيق أولاً قبل اختبار ميزات PWA

#### إضافة الأيقونات لتطبيق admin:

1. أنشئ أيقونات بحجم 192x192 و 512x512 بكسل
2. احفظها كـ PNG في `apps/admin/public/`:
   - `icon-192x192.png`
   - `icon-512x512.png`

أو استخدم الأيقونات من تطبيق www مؤقتاً:
```bash
cp apps/www/public/web-app-manifest-192x192.png apps/admin/public/icon-192x192.png
cp apps/www/public/web-app-manifest-512x512.png apps/admin/public/icon-512x512.png
```

## 🔍 اختبار PWA

### في المتصفح:

1. افتح DevTools (F12)
2. اذهب إلى تبويب **Application**
3. تحقق من:
   - **Manifest**: يجب أن يظهر جميع المعلومات بشكل صحيح
   - **Service Workers**: يجب أن يكون مسجل ويعمل
   - **Storage**: يمكنك رؤية الـ cache

### اختبار التثبيت:

1. افتح الموقع في وضع الإنتاج
2. تحقق من ظهور زر "Install" في المتصفح
3. جرب التثبيت على الجهاز

## 📋 الملفات المعدلة/المضافة

### تطبيق www:
- ✅ `apps/www/app/manifest.ts` - محسّن
- ✅ `apps/www/app/[locale]/layout.tsx` - محدث مع Viewport و Metadata
- ✅ `apps/www/next.config.ts` - محسّن مع runtime caching

### تطبيق admin:
- ✅ `apps/admin/app/manifest.ts` - جديد
- ✅ `apps/admin/app/layout.tsx` - محدث مع PWA metadata
- ✅ `apps/admin/components/register-sw.tsx` - جديد
- ✅ `apps/admin/public/sw.js` - جديد
- ✅ `apps/admin/next.config.ts` - محسّن
- ✅ `apps/admin/PWA_SETUP.md` - دليل إضافة الأيقونات

## 🎯 الميزات المتاحة

- ✅ **التثبيت**: يمكن تثبيت التطبيق على الجهاز
- ✅ **العمل Offline**: Service Worker يخزن الموارد للعمل بدون إنترنت
- ✅ **أيقونة على الشاشة الرئيسية**: يظهر كتطبيق عادي
- ✅ **شاشة كاملة**: يعمل في وضع standalone بدون شريط المتصفح
- ✅ **Caching**: يحفظ الموارد لتحسين الأداء

## 🚀 الخطوات التالية

1. **إضافة الأيقونات لتطبيق admin** (راجع `apps/admin/PWA_SETUP.md`)
2. **اختبار التطبيقات** في وضع الإنتاج
3. **تخصيص Service Worker** حسب الحاجة (اختياري)
4. **إضافة Push Notifications** (اختياري - موجود في www بالفعل)

## 📚 موارد إضافية

- [MDN Web Docs - Progressive Web Apps](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)
- [Next.js PWA Documentation](https://github.com/shadowwalker/next-pwa)
- [Web.dev - PWA](https://web.dev/progressive-web-apps/)

---

تم إعداد PWA بنجاح! 🎉





